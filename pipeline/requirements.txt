# Dependencies are declared in pyproject.toml; this file exists so
# `pip install -r requirements.txt` keeps working. Installs the package in
# editable mode with the dev extras (pytest, ruff, mypy).
-e .[dev]
